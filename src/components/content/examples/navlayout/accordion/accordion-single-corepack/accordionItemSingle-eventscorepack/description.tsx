import 'preact';

export const accordionItemSingleEventscorepackDescription = (
  <>
    <p>
      An accordion item single component renders collapsible content in a group
      to support single expansion, when only one of the items in the group can
      be expanded at a time.
    </p>
    <p>
      This demo shows the events triggered when an{" "}
      <code class="prettyprint">oj-c-accordion-item-single</code> is expanded
      or collapsed. The event log displays the event type, the item key, and
      details about the expanded or collapsed state.
    </p>
  </>
);
