import { h } from "preact";

export const accordionItemSingleBasiccorepackDescription = (
  <>
    <p>
      An accordion item single component renders collapsible content in a group
      to support single expansion, when only one of the items in the group can
      be expanded at a time.
    </p>
    <p>
      This demo shows a basic usage of{" "}
      <code class="prettyprint">oj-c-accordion-item-single</code>. Each item in
      the accordion is represented by an{" "}
      <code class="prettyprint">oj-c-accordion-item-single</code> component.
      Only one item can be expanded at a time.
    </p>
    <p>
      The <code class="prettyprint">expanded-key</code> property controls which
      item is open, and <code class="prettyprint">item-key</code> uniquely
      identifies each item.
    </p>
  </>
);
