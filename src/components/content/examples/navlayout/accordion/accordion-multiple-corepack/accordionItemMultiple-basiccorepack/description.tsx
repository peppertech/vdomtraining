import { h } from "preact";

export const accordionItemMultipleBasiccorepackDescription = (
  <>
    <p>
      An accordion item multiple component renders collapsible content in a
      group to support multiple expansion, when more than one of the items in
      the group can be expanded at a time.
    </p>
    <p>
      This demo shows an accordion that allows multiple items to be expanded at
      once using <code class="prettyprint">oj-c-accordion-item-multiple</code>.
      The <code class="prettyprint">expanded-keys</code> property is an array
      that controls which items are open. Each item must have a unique{" "}
      <code class="prettyprint">item-key</code>.
    </p>
  </>
);
