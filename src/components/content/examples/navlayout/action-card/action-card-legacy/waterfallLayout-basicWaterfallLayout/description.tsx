import { h } from 'preact';

export const waterfallLayoutBasicWaterfallLayoutDescription = (
  <>
    <p>A waterfall layout displays heterogeneous data as a grid of cards.</p>
    <p>Simple Waterfall Layout that shows how to render items from a DataProvider.</p>
  </>
);
