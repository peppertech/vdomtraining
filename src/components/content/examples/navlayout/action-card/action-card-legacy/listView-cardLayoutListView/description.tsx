import { h } from "preact";

export const listViewCardLayoutListViewDescription = (
  <>
    <p>A list view displays data items as a list or a grid with highly interactive features.</p>
    <p>This demo shows an oj-list-view card layout that renders each item with an oj-action-card.</p>
  </>
);
