// @ts-nocheck
import { h } from 'preact';

export const actioncardBasicRecipe = (
  <ol>
    <li>
      Create an <code>oj-action-card</code> element
    </li>
    <li>
      Add an <code>ojAction</code> event listener to the <code>oj-action-card</code> element using
      the `onojAction` attribute.
    </li>
  </ol>
);
