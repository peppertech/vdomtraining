import 'preact';

const descriptionHtmlText = String.raw`<p>A Drawer Layout adds expandable side contents (drawers) alongside some primary content.</p><p>Drawer Layout supports drawers at the 'start', 'end' and 'bottom' edge.</p>
<p>This demo shows how to use them simultaneously.</p>`;

export const drawerLayoutMultipleDrawerscorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
