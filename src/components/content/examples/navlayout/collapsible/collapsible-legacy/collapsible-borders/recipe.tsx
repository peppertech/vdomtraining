// @ts-nocheck
import 'preact';

export const collapsibleBordersRecipe = (
  <>
    <ol>
      <li>add the class "oj-header-border" to the header (h1, h2, h3, h4 tag).</li>
    </ol>
  </>
);
