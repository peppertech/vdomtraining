// @ts-nocheck
import { h } from 'preact';

export const collapsibleBordersDescription = (
  <>
    <p>A collapsible displays a header that can be expanded to show its content.</p>This demo shows what collapsibles look like with a border at different header levels (h1, h2, h3,
    h4).
  </>
);
