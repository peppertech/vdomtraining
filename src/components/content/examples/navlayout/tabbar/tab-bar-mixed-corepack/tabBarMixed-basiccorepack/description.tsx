import 'preact';

const descriptionHtmlText = String.raw`<p>A navigation component that enables horizontal navigation between distinct content with a mixture of static and dynamic tabs.</p>This demo shows a basic oj-c-tab-bar-mixed with static and dynamic tabs with various configurations.`;

export const tabBarMixedBasiccorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
