import 'preact';

const descriptionHtmlText = String.raw`<p>A navigation component that enables horizontal navigation between distinct content with a mixture of static and dynamic tabs.</p>This demo shows how to add responsive behavior to oj-c-tab-bar-mixed.`;

export const tabBarMixedResponsivecorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
