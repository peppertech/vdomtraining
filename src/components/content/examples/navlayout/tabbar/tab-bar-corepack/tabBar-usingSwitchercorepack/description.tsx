import 'preact';

const descriptionHtmlText = String.raw`<p>A tab bar allows navigation between different content sections.</p><p>This demo shows how to use oj-switcher to show/hide oj-c-tab-bar content section.</p>`;

export const tabBarUsingSwitchercorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
