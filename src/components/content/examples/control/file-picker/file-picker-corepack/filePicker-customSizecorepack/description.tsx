import 'preact';

const descriptionHtmlText = String.raw`<p>A file picker displays a clickable dropzone for selecting files from the device storage.</p>
<p>This demo shows how to change the width and height of an oj-c-file-picker component.</p>`;

export const filePickerCustomSizecorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
