import 'preact';

export const progressBarOverviewRecipe = (
  <>
    <ol>
      <li>
        Create an <code>oj-progress-bar</code> element with a defined <a href={'#'}>value</a>{' '}
        attribute.
      </li>
      <li>Review the basic demo to see determinate and indeterminate progress bars.</li>
      <li>Review the loading demo to see a loading progress bar tied to changing data.</li>
      <li>
        Use the <code>oj-progress-bar-embedded</code> class to embed a progress bar inside another
        container.
      </li>
    </ol>
  </>
);
