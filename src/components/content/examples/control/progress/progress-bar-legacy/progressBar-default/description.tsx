import { h } from 'preact';

export const progressBarDefaultDescription = (
  <>
    <p>
      A progress bar allows the user to visualize the progression of an extended computer
      operation.
    </p>
    <p>
      This demo of <code>oj-progress-bar</code> uses <code>oj-input-number</code> to define the
      current value. Use the Indeterminate checkbox to switch the progress bar to an indeterminate
      state.
    </p>
  </>
);
