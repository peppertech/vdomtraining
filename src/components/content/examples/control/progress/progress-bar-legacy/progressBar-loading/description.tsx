import 'preact';

export const progressBarLoadingDescription = (
  <>
    <p>
      A progress bar allows the user to visualize the progression of an extended computer
      operation.
    </p>
    <p>
      This demo shows an <code>oj-progress-bar</code> that is linked to changing data. Follow the
      recipe to understand how to use the <code>aria-describedby</code> and <code>aria-busy</code>{' '}
      attributes for accessibility. Click Restart to run the loading sequence again.
    </p>
  </>
);
