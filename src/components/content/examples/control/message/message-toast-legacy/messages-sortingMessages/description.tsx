// @ts-nocheck
import 'preact';

export const messagesSortingMessagesDescription = (
  <>
    <p>Messages manages the layout and display of child messages.</p><p>
      This features sorting of messages based on the 'severity' property of the messages data, when
      using DataProvider. In similar lines, sorting could be based on other properties like 'summary'
      and 'timestamp'.
    </p>
  </>
);
