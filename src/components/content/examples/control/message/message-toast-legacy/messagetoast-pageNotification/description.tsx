import { h } from "preact";

export const messagetoastPageNotificationDescription = (
  <>
    <p>Messages manages the layout and display of child messages.</p>
    <p>
      This demo shows toast notification messages in a page with configurable position, display
      options, and message content.
    </p>
  </>
);
