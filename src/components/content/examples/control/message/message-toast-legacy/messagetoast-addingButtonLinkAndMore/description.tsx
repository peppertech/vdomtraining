// @ts-nocheck
import { h } from 'preact';

export const messagetoastAddingButtonLinkAndMoreDescription = (
  <>
    <p>Messages manages the layout and display of child messages.</p><p>
      This demo features adding controls like buttons, links or any other JET components to messages
      using the 'messsageTemplate' slot on
      <code className={"prettyprint"}>oj-messages</code>
      and 'detail' slot on
      <code className={"prettyprint"}>oj-message</code>
      .
    </p>
  </>
);
