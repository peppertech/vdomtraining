import { h } from "preact";

export const messagetoastAutoTimeoutDescription = (
  <>
    <p>Messages manages the layout and display of child messages.</p>
    <p>
      This demo shows how to configure notification messages with different auto timeout values.
    </p>
  </>
);
