import 'preact';

const descriptionHtmlText = String.raw`<p>A menu button launches a menu when clicked.</p>This demo features a JET Menu Button with a single menu and menu having submenus.`;

export const menuButtonsMenuButtoncorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
