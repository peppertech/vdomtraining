import 'preact';

const recipeHtmlText = String.raw`<ol>
  <li>Use display attribute and slots to create Icon menu button.</li>
  <li>
    See
    <a href="jsdocs/oj-c.MenuButton.html#display">Display</a>
    section in the API doc for more details.
  </li>
</ol>`;

export const menuButtonsMenuButtonDisplaycorepackRecipe = (
  <div dangerouslySetInnerHTML={{ __html: recipeHtmlText }} />
);
