import 'preact';

const descriptionHtmlText = String.raw`<p>A menu button launches a menu when clicked.</p>This demo shows how icons are displayed.`;

export const menuButtonsMenuButtonDisplaycorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
