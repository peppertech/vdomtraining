import 'preact';

const descriptionHtmlText = String.raw`<p>A Split Menu Button combines a push button and menu button.</p><p>
  This demo shows a split menu button, oj-c-split-menu-button, which combines an action button and menu button.
</p>`;

export const splitmenubuttonOverviewcorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
