import { h } from "preact";

const descriptionHtmlText = String.raw`<p>A Buttonset Multiple allows a user to select the states of one or more related options.</p><p>
  This demo shows the more important visual aspects of
  <code>oj-c-buttonset-multiple</code>
  .
</p>`;

export const buttonsetmultipleOverviewcorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
