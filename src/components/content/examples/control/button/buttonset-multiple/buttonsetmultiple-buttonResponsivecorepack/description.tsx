import { h } from "preact";

const descriptionHtmlText = String.raw`<p>A Buttonset Multiple allows a user to select the states of one or more related options.</p><p>This demo shows some strategies for making a Buttonset responsive.</p>`;

export const buttonsetmultipleButtonResponsivecorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
