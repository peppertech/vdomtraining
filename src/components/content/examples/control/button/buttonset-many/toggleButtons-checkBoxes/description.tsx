// @ts-nocheck
import { h } from 'preact';

export const toggleButtonsCheckBoxesDescription = (
  <>
    <p>A buttonset many is a grouping of related buttons where any number of buttons may be selected.</p><p>
      This demo features a "select many" Buttonset, i.e. a Buttonset where any number of the buttons can
      be toggled.
    </p>

    <p>Such a grouping is appropriate when the buttons are semantically related.</p>
  </>
);
