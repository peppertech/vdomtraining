// @ts-nocheck
import { h } from 'preact';

export const toggleButtonsButtonIconsDescription = (
  <>
    <p>An option represents a value for JET elements that display a list of values.</p>
    <p>JET Buttons with various combinations of text and icons, specified via the component API.</p>
    <p>Note that the icon-only buttons display the label in a tooltip.</p>
  </>
);
