// @ts-nocheck
import { h } from "preact";

export const buttonsetoneBasicRecipe = (
  <>
    <ol>
      <li>
        Create an <code className={"prettyprint"}>&lt;oj-buttonset-one&gt;</code> element.
      </li>
      <li>
        Add one <code className={"prettyprint"}>&lt;oj-option&gt;</code> for each selectable button.
      </li>
      <li>
        Set <code className={"prettyprint"}>value</code> to the selected option and add an accessible
        label with <code className={"prettyprint"}>aria-label</code> or
        <code className={"prettyprint"}>labelled-by</code>.
      </li>
    </ol>
  </>
);
