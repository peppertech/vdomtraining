import { h } from "preact";

const descriptionHtmlText = String.raw`<p>Buttons direct users to initiate or take actions and work with a single tap, click, or keystroke.</p>This demo shows the ojAction event supported by oj-c-button.`;

export const buttonsEventscorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
