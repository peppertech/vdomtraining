import 'preact';

export const pushButtonsButtonformcontrolsRecipe = (
  <>
    <ol>
      <li>Place the form control and push button in the same horizontal group.</li>
      <li>Use top labels when aligning controls inside an `oj-label-value` layout.</li>
      <li>Use a large button when pairing a push button with controls that use inside labels.</li>
    </ol>
  </>
);
