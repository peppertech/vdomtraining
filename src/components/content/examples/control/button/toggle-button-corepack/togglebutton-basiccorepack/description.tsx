import 'preact';

const descriptionHtmlText = String.raw`<p>Toggle Buttons allow users to switch between states when clicked or tapped.</p>This demo shows binding the value in oj-c-toggle-button.`;

export const togglebuttonBasiccorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
