import 'preact';

export const trainLinearDescription = (
  <>
    <p>A train allows a user to navigate between different steps of a process.</p>
    <p>
      Click on a step or label to move to that step. This demo shows a linear train, meaning for unvisited steps
      the user can move forward only one step, they cannot skip forward two or more steps.
    </p>
  </>
);
