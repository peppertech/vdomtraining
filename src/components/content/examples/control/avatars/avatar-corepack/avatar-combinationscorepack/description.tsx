import 'preact';

const descriptionHtmlText = String.raw`<p>An avatar represents a person or entity as initials or an image.</p>  <p>
    This demo shows how to display initials, images, icons, and placeholders in an avatar
    component.
  </p>`;

export const avatarCombinationscorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
