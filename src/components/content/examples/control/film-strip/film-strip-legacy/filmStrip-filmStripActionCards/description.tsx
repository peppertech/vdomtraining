import 'preact';

export const filmStripFilmStripActionCardsDescription = (
  <>
    <p>
      A filmstrip lays out its children in a single row or column across logical pages and allows
      navigating through them.
    </p>
    <p>
      This demo shows <code className="prettyprint">oj-film-strip</code> with action cards. An
      action card renders provided content, and in this example it shows a profile card layout.
    </p>
  </>
);
