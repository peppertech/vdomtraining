// @ts-nocheck
import { h } from 'preact';

export const filmStripFilmStripLazyRenderingDescription = (
  <>
    <p>A filmstrip lays out its children in a single row or column across logical pages and allows navigating through them.</p>This demo showcases an oj-film-strip that renders content lazily.
  </>
);
