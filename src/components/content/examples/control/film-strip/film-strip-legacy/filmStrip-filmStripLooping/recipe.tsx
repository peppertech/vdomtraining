// @ts-nocheck
import { h } from 'preact';

export const filmStripFilmStripLoopingRecipe = (
  <>
    HTML Markup:
    <ol>
      <li>
        Create an
        <code className={"prettyprint"}>oj-film-strip</code>
        element.
      </li>
      <li>In the HTML, specify a group of sibling elements to be laid out by the oj-film-strip.</li>
      <li>
        Set the
        <code className={"prettyprint"}>looping</code>
        attribute to
        <code className={"prettyprint"}>'page'</code>
        to allow cycling through the pages so that navigation doesn't stop in one direction.
      </li>
    </ol>
  </>
);
