// @ts-nocheck
import 'preact';

export const toolbarsToolbarDescription = (
  <>
    <p>A toolbar displays a strip of control elements such as buttons and menu buttons, often grouped by separators.</p>This demo shows a WAI-ARIA-compliant toolbar, with arrow key navigation and a single tabstop. The
    tabstop updates on navigation, so that tabbing back into the toolbar returns to the most recently
    focused button.
  </>
);
