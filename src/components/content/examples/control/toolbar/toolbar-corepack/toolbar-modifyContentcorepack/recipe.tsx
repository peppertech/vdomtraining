import 'preact';

const recipeHtmlText = String.raw`<ol>
  <li>Programmatically altar toolbar content.</li>
</ol>`;

export const toolbarModifyContentcorepackRecipe = (
  <div dangerouslySetInnerHTML={{ __html: recipeHtmlText }} />
);
