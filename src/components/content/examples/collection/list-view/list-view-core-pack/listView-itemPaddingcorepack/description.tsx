import 'preact';

const descriptionHtmlText = String.raw`<p>A list view displays data items as a list or a grid with highly interactive features.</p>This demo shows how to enable padding around each item in oj-c-list-view element without using oj-c-list-item-layout.  Note that oj-c-list-item-layout is the recommended component to use to handle complex layout.`;

export const listViewItemPaddingcorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
