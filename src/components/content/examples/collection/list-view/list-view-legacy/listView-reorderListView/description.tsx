// @ts-nocheck
import { h } from 'preact';

export const listViewReorderListViewDescription = (
  <>
    <p>A list view displays data items as a list or a grid with highly interactive features.</p><p>Reorder items within ListView. Use dragEnter callback to disallow drop on folders.</p>

    <p>
      To reorder items, simply use the drag handle on the item, drag it and drop it onto the desired
      location with the ListView.
    </p>

    <p>
      To reorder items using keyboard, navigate to the item that you want to move. Press Ctrl (or
      Command key) + X key. Navigate to the desired location within the ListView. Press Ctrl (or Command
      key) + V key to complete the operation.
    </p>
  </>
);
