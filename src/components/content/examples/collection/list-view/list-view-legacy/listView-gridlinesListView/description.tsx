// @ts-nocheck
import 'preact';

export const listViewGridlinesListViewDescription = (
  <>
    <p>A list view displays data items as a list or a grid with highly interactive features.</p>This demo shows how to show and hide gridlines in ListView. By default, whether gridlines are
    visible or hidden depends on the theme, but can be explicitly set using the gridlines attribute.
  </>
);
