// @ts-nocheck
import { h } from 'preact';

export const listViewStickyGroupHeaderListViewDescription = (
  <>
    <p>A list view displays data items as a list or a grid with highly interactive features.</p>This demo shows how to make group header stick to the top when user scrolls in ListView.
  </>
);
