// @ts-nocheck
import { h } from 'preact';

export const listViewWithTableDndListViewDescription = (
  <>
    <p>A list view displays data items as a list or a grid with highly interactive features.</p><p>This demo shows how to setup drag and drop between two ListViews.</p>

    <p>
      To drag data from one ListView and drop it on another ListView, simply use the drag handle on the
      item, drag and drop it onto the desired location on the other ListView.
    </p>

    <p>
      To move data from one ListView to another using keyboard, navigate to the item that you want to
      move. Press Ctrl (or Command key) + X key. Navigate to the desired location on the other ListView.
      Press Ctrl (or Command key) + V key to complete the move operation.
    </p>
  </>
);
