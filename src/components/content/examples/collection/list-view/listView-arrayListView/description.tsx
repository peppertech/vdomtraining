import { h } from 'preact';

export const listViewArrayListViewDescription = (
  <>
    <p>A list view displays data items as a list or a grid with highly interactive features.</p>This demo shows a ListView with single selection enabled and how to access information about the
    selected item. It also shows how to enforce an item selected.
  </>
);
