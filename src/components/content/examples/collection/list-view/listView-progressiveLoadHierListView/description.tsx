// @ts-nocheck
import { h } from 'preact';

export const listViewProgressiveLoadHierListViewDescription = (
  <>
    <p>A list view displays data items as a list or a grid with highly interactive features.</p>This demo shows a ListView with an initial loading indicator, the loading indicator also appears
    when expanding a group item and scrolling. ListView only shows the loading indicator after a
    pre-defined time has elapsed during the data provider fetch.
  </>
);
