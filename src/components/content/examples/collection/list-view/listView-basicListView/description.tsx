import { h } from 'preact';

export const listViewBasicListViewDescription = (
  <>
    <p>A list view displays data items as a list or a grid with highly interactive features.</p>This demo shows a ListView with an item template against an array of data.
  </>
);
