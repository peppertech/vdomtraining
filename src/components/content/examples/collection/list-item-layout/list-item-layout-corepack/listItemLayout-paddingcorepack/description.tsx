import 'preact';

const descriptionHtmlText = String.raw`<p>A List Item Layout represents layout used for list view item elements.</p>The demo shows how to render layout content using using a list view layout component with no outer padding.`;

export const listItemLayoutPaddingcorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
