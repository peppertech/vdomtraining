const loadedSource = require("!!raw-loader!./listItemLayout-trailingSlotcorepack.tsx");
const source: string =
  typeof loadedSource === "string" ? loadedSource : loadedSource.default;

export default source;
