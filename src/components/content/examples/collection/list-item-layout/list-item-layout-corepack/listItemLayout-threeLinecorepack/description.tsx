import 'preact';

const descriptionHtmlText = String.raw`<p>A List Item Layout represents layout used for list view item elements.</p>The demo shows how to render three line layout content using default, secondary and tertiary slots
within list view using a list view layout component.`;

export const listItemLayoutThreeLinecorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
