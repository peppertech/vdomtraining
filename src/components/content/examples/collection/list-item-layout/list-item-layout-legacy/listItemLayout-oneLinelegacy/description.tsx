import 'preact';

const descriptionHtmlText = String.raw`<p>A List Item Layout represents layout used for list view item elements.</p>The demo shows how to render one line layout content using default slot within list view using a
list view layout component.`;

export const listItemLayoutOneLinelegacyDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
