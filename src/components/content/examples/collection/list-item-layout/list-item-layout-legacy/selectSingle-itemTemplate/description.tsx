// @ts-nocheck
import { h } from 'preact';

export const selectSingleItemTemplateDescription = (
  <>
    <p>A List Item Layout represents layout used for list view item elements.</p>
    <p>
      This demo shows how to use an <code>oj-list-item-layout</code> inside the
      {" "}
      <code>itemTemplate</code> slot of <code>oj-select-single</code> to render richer dropdown
      options with an avatar, primary text, secondary text, and metadata.
    </p>
  </>
);
