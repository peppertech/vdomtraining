import 'preact';

const descriptionHtmlText = String.raw`<p>A List Item Layout represents layout used for list view item elements.</p>The demo shows how to render overline content within list view using a list view layout component.`;

export const listItemLayoutOverlineSlotlegacyDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
