const loadedSource = require("!!raw-loader!./listItemLayout-quaternarySlotlegacy.tsx");
const source: string =
  typeof loadedSource === "string" ? loadedSource : loadedSource.default;

export default source;
