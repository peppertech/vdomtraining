// @ts-nocheck
import 'preact';

export const bufferingTreeDataProviderTableRowExpanderMutationsDescription = (
  <>
    <p>
      This demo shows how to mutate data in a Tree Table. To start performing mutations, select a row;
      then you can add a child, add a sibling, remove, or update the selected row.
    </p>
  </>
);
