import 'preact';

export const pullToRefreshBasicPullToRefreshDescription = (
  <>
    <p>A refresher is a wrapper for attaching pull-to-refresh functionality to a DOM element.</p>
    <p>
      This demo shows how to add a Refresher to ListView with a collection-based data source to
      provide pull-to-refresh capability.
    </p>
    <p>
      <span class="oj-text-color-danger">NOTE:</span> Pull-to-refresh only works on touch devices.
    </p>
  </>
);
