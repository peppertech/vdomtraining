export const indexerCharacterIndexerRecipe = (
  <>
    <ol>
      <li>Create a ListView using the JSON data source shown in the demo.</li>
      <li>
        Create an IndexerModelTreeDataProvider and apply it to both the JET ListView and the JET
        Indexer using the <code>data</code> property.
      </li>
      <li>Use the id of the ListView as the value of the indexer&apos;s <code>aria-controls</code> attribute.</li>
      <li>Use flex layout to position the ListView and Indexer.</li>
      <li>Apply bindings or render the equivalent VDOM component.</li>
    </ol>
  </>
);
