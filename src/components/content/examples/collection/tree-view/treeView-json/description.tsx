import { h } from 'preact';

export const treeViewJsonDescription = (
  <>
    <p>A tree view displays hierarchical relationships between items.</p>
    <p>This demo shows how to populate a TreeView using JSON data and an ArrayTreeDataProvider.</p>
  </>
);
