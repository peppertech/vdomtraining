import 'preact';

export const treeViewDndDescription = (
  <>
    <p>A tree view displays hierarchical relationships between items.</p>
    <p>This demo shows how TreeView supports drag and drop to and from any component.</p>

    <p>
      Drag items from the "Drag Source" into the "Drop Target". The drag sources in this demo include
      three simple divs and a TreeView.
    </p>

    <p>
      To move item using keyboard, navigate to the item or the simple divs that you wanted to move. Use
      Ctrl (or Command) + X key on the item/div. Navigate to where you want the item to move to. Use
      Ctrl (or Command) + V key to complete the move.
    </p>
  </>
);
