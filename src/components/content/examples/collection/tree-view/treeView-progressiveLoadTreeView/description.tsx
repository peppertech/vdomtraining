import 'preact';

export const treeViewProgressiveLoadTreeViewDescription = (
  <>
    <p>A tree view displays hierarchical relationships between items.</p>
    <p>
      This demo shows a Treeview with an initial loading indicator, the loading indicator
      also appears when expanding a group item. Treeview only shows the loading indicator
      after a pre-defined time has elapsed during the data provider fetch.
    </p>
  </>
);
