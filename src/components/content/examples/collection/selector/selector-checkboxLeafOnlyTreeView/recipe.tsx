// @ts-nocheck
import { h } from 'preact';

export const selectorCheckboxLeafOnlyTreeViewRecipe = (
  <>
    <ul>
      <li>Create an <code>oj-tree-view</code> backed by an <code>ArrayTreeDataProvider</code>.</li>
      <li>Render <code>oj-selector</code> from the tree item template.</li>
      <li>Bind the shared selected KeySet to both the tree and the selector controls.</li>
    </ul>
  </>
);
