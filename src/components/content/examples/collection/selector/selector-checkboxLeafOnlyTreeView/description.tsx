// @ts-nocheck
import 'preact';

export const selectorCheckboxLeafOnlyTreeViewDescription = (
  <>
    <p>The selector component renders checkboxes in collections to support selection.</p>
    <p>
      This demo shows how to create a checkbox tree using a treeView item template with cascaded
      selection.
    </p>
    <ul>
      <li>If a parent item is selected, all descendants are selected automatically.</li>
      <li>If a parent item is unselected, all descendants are unselected automatically.</li>
      <li>Mixed descendants can produce a partially selected parent state.</li>
    </ul>
  </>
);
