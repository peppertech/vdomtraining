// @ts-nocheck
import 'preact';

export const selectorCheckboxSelectableTreeViewRecipe = (
  <>
    <ul>
      <li>Back the tree with an <code>ArrayTreeDataProvider</code> and a shared selected KeySet.</li>
      <li>Render <code>oj-selector</code> inside the tree item template.</li>
      <li>Use an external radioset to switch the tree view selection mode.</li>
    </ul>
  </>
);
