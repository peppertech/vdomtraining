// @ts-nocheck
import { h } from 'preact';

export const selectorListviewCheckboxCardDescription = (
  <>
    <p>The selector component renders checkboxes in collections to support selection.</p>Simple example to show how the selector can be used with ListView Card Layout.
  </>
);
