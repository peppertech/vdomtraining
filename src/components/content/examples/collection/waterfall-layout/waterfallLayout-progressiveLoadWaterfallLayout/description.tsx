import 'preact';

export const waterfallLayoutProgressiveLoadWaterfallLayoutDescription = (
  <>
    <p>A waterfall layout displays heterogeneous data as a grid of cards.</p>
    <p>
      This demo shows a WaterfallLayout with an initial loading indicator, the loading indicator also
      appears when scrolling. WaterfallLayout only shows the loading indicator after a pre-defined time
      has elapsed during the data provider fetch.
    </p>
  </>
);
