// @ts-nocheck
import 'preact';

export const swipeToRevealBasicSwipeToRevealDescription = (
  <>
    <p>A swipe actions component adds swipe-to-reveal functionality to elements such as items in ListView.</p>Sets up swipe to reveal on each items in ListView. When a swipe action is invoked, the text above
    ListView will be updated accordingly. When a full trigger is invoked by swiping all the way to the
    left (or right in right-to-left reading direction), the default action will be invoked.
  </>
);
