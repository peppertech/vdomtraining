// @ts-nocheck
import 'preact';

export const dataGridFreezeGridDescription = (
  <>
    <p>A data grid displays data in a cell oriented grid.</p><p>
      This demo shows how to enable row and column freeze and unfreeze functionality. Use the context
      menu on headers and cells to modify the frozen regions. Initialize the frozen state using
      write-back properties for frozen row and column count. Interact with the controls below to change
      the frozen count on rows and columns.
    </p>
  </>
);
