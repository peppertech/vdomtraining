// @ts-nocheck
import { h } from 'preact';

export const dataGridProgressiveLoadingDescription = (
  <>
    <p>A data grid displays data in a cell oriented grid.</p><p>
      This demo shows a Datagrid with an initial loading indicator, the loading indicator also appears
      when scrolling. Datagrid only shows the loading indicator after a pre-defined time has elapsed
      during the data provider fetch.
    </p>
  </>
);
