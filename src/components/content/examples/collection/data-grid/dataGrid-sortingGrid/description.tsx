// @ts-nocheck
import { h } from 'preact';

export const dataGridSortingGridDescription = (
  <>
    <p>A data grid displays data in a cell oriented grid.</p><p>
      This demo shows how to implement sorting on a DataGrid. Sort icons and gestures are supported
      internally by the DataGrid component. The application's responsibility is to respond to the sort
      event from the DataGrid by firing a DataGridProvider refresh event with a sort flag once the
      DataGridProvider is ready to have the sorted data fetched.
    </p>
  </>
);
