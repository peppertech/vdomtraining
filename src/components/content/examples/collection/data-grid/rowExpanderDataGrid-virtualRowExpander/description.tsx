// @ts-nocheck
import { h } from 'preact';

export const rowExpanderDataGridVirtualRowExpanderDescription = (
  <>
    <p>Enable hierarchical data to be displayed in a JET Table.</p><p>
      This demo shows how to populate a Data Grid with Row Expanders using a hierarchical set of virtual
      oj.Collections.
    </p>
  </>
);
