// @ts-nocheck
import { h } from 'preact';

export const dataGridHideAxisGridDescription = (
  <>
    <p>A data grid displays data in a cell oriented grid.</p><p>
      This demo shows how to set columns as hidden initially and enable end user hide/show on the data
      grid. In this demo columns 0 and 5 are initially hidden. Use the context menu to hide and show
      columns based on selection. Use the select drop down to simulate programmatic hide and show.
    </p>
  </>
);
