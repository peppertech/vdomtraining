// @ts-nocheck
import { h } from 'preact';

export const dataGridScrollPositionGridDescription = (
  <>
    <p>A data grid displays data in a cell oriented grid.</p><p>
      This demo shows the use of scrollPosition attribute to track and set the vertical and horizontal
      scroll position of the grid. Update the input fields to scroll the data grid or drag the scroll
      bar to show the current scroll position such as index and pixel.
    </p>
    <p>
      The scrollPosition value can be specified by pixel, index or key. Specifying an offset works off
      of the key and index values.
    </p>
  </>
);
