// @ts-nocheck
import { h } from 'preact';

export const dataGridPerformanceGridDescription = (
  <>
    <p>A data grid displays data in a cell oriented grid.</p><p>This demo shows how data grid performs when processing data sets of different sizes</p>
    <p>
      Interact with the controls below to change the data set size by changing the rows/columns per
      level and row/column header levels and scroll across the data to see how efficiently data grid
      renders the data. Click on render grid to get initial render times.
    </p>
  </>
);
