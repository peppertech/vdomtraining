// @ts-nocheck
import 'preact';

export const dataGridMergeCellsGridDescription = (
  <>
    <p>A data grid displays data in a cell oriented grid.</p><p>
      This demo shows how one could use a oj-data-grid with a custom datagrid provider to merge cells by
      passing an array of merge values to it.
    </p>
  </>
);
