// @ts-nocheck
import 'preact';

export const dataGridDragAndDropGridDescription = (
  <>
    <p>A data grid displays data in a cell oriented grid.</p><p>
      This demo shows how to enable row and column drag and drop functionality for reordering. Select
      rows or columns via the headers and use the headers as drag targets. Locate the drop target via
      the drop line indicator. To use the keyboard use the cut event in conjunction with the insert
      rows/columns context menu item. This functionality can be extended by applications to support drag
      and drop to/from external sources.
    </p>
  </>
);
