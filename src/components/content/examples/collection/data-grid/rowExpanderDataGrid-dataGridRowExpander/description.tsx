// @ts-nocheck
import 'preact';

export const rowExpanderDataGridDataGridRowExpanderDescription = (
  <>
    <p>Enable hierarchical data to be displayed in a JET Table.</p><p>
      The Row Expander is a component which, when included in a cell (DataGrid) or column (Table),
      enables user to easily expand and collapse a row. The component renders the expand/collapse icon
      with the appropriate indentation, and works directly with the flattened data source. Display
      hierarchical data using row expander with data grid. Note the row expander icon can be stamped in
      row headers or data body cells.
    </p>
  </>
);
