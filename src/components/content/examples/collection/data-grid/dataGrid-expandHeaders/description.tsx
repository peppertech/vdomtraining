// @ts-nocheck
import { h } from 'preact';

export const dataGridExpandHeadersDescription = (
  <>
    <p>
      This demo shows how to declare nested headers in the RowDataGridProvider. Nested headers can be
      declared up front or set as a function callback based on the data body columns in the
      columnHeaders initialization option. The header labels shown on the nested headers are set in a
      similar fashion.
    </p>
  </>
);
