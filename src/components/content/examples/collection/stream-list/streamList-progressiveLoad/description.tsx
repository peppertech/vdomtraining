// @ts-nocheck
import 'preact';

export const streamListProgressiveLoadDescription = (
  <>
    <p>A stream list displays data in an activity stream feed.</p>This demo shows a StreamList with an initial loading indicator, the loading indicator also appears
    when expanding a group item and scrolling. StreamList only shows the loading indicator after a
    pre-defined time has elapsed during the data provider fetch.
  </>
);
