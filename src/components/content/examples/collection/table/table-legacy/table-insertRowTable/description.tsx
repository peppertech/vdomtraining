// @ts-nocheck
import 'preact';

export const tableInsertRowTableDescription = (
  <>
    <p>A table displays data items in a tabular format with highly interactive features.</p>
    <p>This demo shows how to configure an 'insert row' row in the table which allows users to easily create a new data row in a specific location.</p>
    <p>Editing, add-row, and insert-row are handled asynchronously. Use the controls below to simulate validation delay for entering and submitting edits/adds/inserts. The Table waits for the validation promise; if inputs are invalid, editing stays active and focus returns to the first invalid field.</p>
  </>
);
