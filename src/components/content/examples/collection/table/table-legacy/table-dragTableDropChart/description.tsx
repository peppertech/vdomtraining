// @ts-nocheck
import 'preact';

export const tableDragTableDropChartDescription = (
  <>
    <p>A table displays data items in a tabular format with highly interactive features.</p>
    <p>To drag data from a table and drop it on a chart:</p>
    <p>First select row(s) which you want to drag, then drag the selected rows onto the chart below.</p>
    <p>To use keyboard to copy and paste data from table to chart:</p>
    <p>Tab to the table on top and navigate to a row with arrow key. Select the row(s) which you want to copy using spacebar key. Use Ctrl (or Command key) + C key to copy the selected row(s). Tab to the chart below. Use Ctrl (or Command key) + V key to paste the selected data that was copied.</p>
  </>
);
