// @ts-nocheck
import 'preact';

export const tableRowReorderTableDescription = (
  <>
    <p>A table displays data items in a tabular format with highly interactive features.</p>
    <p>Reordering the rows in a Table.</p>
    <p>First select row, then drag and drop it to a row position where you want to move it. Drop target is highlighted with empty row.</p>
    <p>To use keyboard to reorder rows: Tab to the table and navigate to a row with arrow key. Select the row which you want to move using spacebar key. Use Ctrl (or Command key) + X key to cut the selected row. Now navigate to the row to paste the selected data. Use Ctrl (or Command key) + V key to paste the selected data that was copied.</p>
  </>
);
