// @ts-nocheck
import 'preact';

export const tableAddRowTableDescription = (
  <>
    <p>A table displays data items in a tabular format with highly interactive features.</p>
    <p>This demo shows how to configure an 'add row' row at the top of the table which allows users to easily create new data rows.</p>
    <p>Editing and add-row are handled asynchronously. Use the controls below to simulate validation delay for entering and submitting edits/adds. The Table waits for the validation promise; if inputs are invalid, editing stays active and focus returns to the first invalid field.</p>
  </>
);
