// @ts-nocheck
import { h } from 'preact';

export const tableComputedFieldDescription = (
  <>
    <p>A table displays data items in a tabular format with highly interactive features.</p>
    <p>This demo shows an editable Table with a client-side computed field (Total = Salary + Bonus).</p>
    <p>Editing is handled asynchronously. The Table waits for the validation promise; if inputs are invalid, editing stays active and focus returns to the first invalid field.</p>
  </>
);
