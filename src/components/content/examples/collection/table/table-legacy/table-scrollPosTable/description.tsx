// @ts-nocheck
import 'preact';

export const tableScrollPosTableDescription = (
  <>
    <p>A table displays data items in a tabular format with highly interactive features.</p>
    <p>Use the scrollPosition attribute to track and set the vertical and horizontal scroll position of the table. Using the scroll bars will update the values of the input fields and vice versa.</p>
    <p>The scrollPosition value can be specified by pixel, index or key. Specifying an offset works off of the key and index values. The keys in this demo are the index value preceded by the letter 'r' for row, or 'c' for column.</p>
  </>
);
