// @ts-nocheck
import { h } from 'preact';

export const tableNavigateTableDescription = (
  <>
    <p>A table displays data items in a tabular format with highly interactive features.</p>
    <p>This demo shows how to save the scroll position, current row and selection state while navigating away from the table and restore them when the user navigates back.</p>
  </>
);
