// @ts-nocheck
import { h } from 'preact';

export const tableFrozenColumnTableDescription = (
  <>
    <p>A table displays data items in a tabular format with highly interactive features.</p>
    <p>This demo shows a table with frozen columns, to see the behavior scroll the table content horizontally. In this demo the first, third, and last columns are configured to freeze at each edge. Note that the selector checkbox column is always frozen.</p>
  </>
);
