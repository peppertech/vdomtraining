// @ts-nocheck
import { h } from 'preact';

export const tableGroupTableDescription = (
  <>
    <p>A table displays data items in a tabular format with highly interactive features.</p>
    <p>This demo shows following table features:</p>
    <br />
    <ul>
      <li>Formatting and ensuring accessibility in a table with grouped data</li>
      <li>Making group header rows sticky</li>
      <li>Disabling selection on group header rows</li>
    </ul>
  </>
);
