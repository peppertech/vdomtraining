// @ts-nocheck
import 'preact';

export const tableDragDropTableDescription = (
  <>
    <p>A table displays data items in a tabular format with highly interactive features.</p>
    <p>To drag data from one table and drop it on another table: First select row(s) which you want to drag, then drag the selected rows onto the table below. Locate the drop target using the highlighted empty row.</p>
    <p>To use keyboard to cut and paste data from one table to another table: Tab to the table on top and navigate to a row with arrow key. Select the row(s) which you want to cut using spacebar key. Use Ctrl (or Command key) + X key to cut the selected row(s). Tab to the table below and navigate to the row to paste the selected data. Use Ctrl (or Command key) + V key to paste the selected data that was copied.</p>
    <p>Note: Rows can't be dragged from Table 2</p>
  </>
);
