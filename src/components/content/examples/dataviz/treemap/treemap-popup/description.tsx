// @ts-nocheck
import 'preact';

export const treemapPopupDescription = (
  <>
    <p>A tree map is an interactive data visualization in which hierarchical data is represented across two dimensions by the size and color of nested rectangular nodes.</p><p>
      This demo shows how to trigger popups when clicking on treemap nodes using a simple oj-popup.
      Applications however, should only use popups instead of built-in component tooltips if additional
      interaction is needed and not just for textual information. Tooltips should be used for
      non-interactive content.
    </p>
  </>
);
