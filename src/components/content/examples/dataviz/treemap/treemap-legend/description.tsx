// @ts-nocheck
import { h } from 'preact';

export const treemapLegendDescription = (
  <>
    <p>A tree map is an interactive data visualization in which hierarchical data is represented across two dimensions by the size and color of nested rectangular nodes.</p><p>
      Treemaps currently do not render with an accompanying legend, but can be configured to correspond
      with an oj-legend element
    </p>
  </>
);
