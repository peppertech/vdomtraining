// @ts-nocheck
import 'preact';

export const treemapProgressiveLoadingDescription = (
  <>
    <p>A tree map is an interactive data visualization in which hierarchical data is represented across two dimensions by the size and color of nested rectangular nodes.</p><p>
      This demo shows the progressive loading indicator when the data provider fetch takes longer than a
      certain time. The appearance of the loading indicator varies by aspect ratios.
    </p>
  </>
);
