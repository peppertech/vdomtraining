// @ts-nocheck
import { h } from 'preact';

export const treemapContextMenuDescription = (
  <>
    <p>A tree map is an interactive data visualization in which hierarchical data is represented across two dimensions by the size and color of nested rectangular nodes.</p><p>
      This demo shows how to trigger context menus on treemap nodes. Right click on a node and select an
      action. The context menu can also be accessed by keyboard. Press Shift + F10 to open the context
      menu after navigating to a node. Select one of the actions by navigating to it and pressing Enter.
    </p>
  </>
);
