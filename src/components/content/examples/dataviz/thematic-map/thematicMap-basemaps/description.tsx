// @ts-nocheck
import { h } from 'preact';

export const thematicMapBasemapsDescription = (
  <>
    <p>A thematic map is an interactive data visualization that displays data corresponding to geographic locations or regions.
                 Applications are required to supply a mapProvider for a valid thematic map.</p><p>
      This demo shows how to set the
      <code>map-provider</code>
      attribute using publicly available geographic data. Thematic Map supports rendering of GeoJSON
      geographic areas which the application sets via the
      <code>map-provider</code>
      attribute. For higher fidelity in border shapes, provide the component with higher fidelity
      basemap data.
    </p>
  </>
);
