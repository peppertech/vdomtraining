// @ts-nocheck
import { h } from 'preact';

export const thematicMapAnimationRecipe = (
  <>
    <ul>
      <li>
        Set the
        <code>animation-on-data-change</code>
        attribute to 'auto' to see data items animate when data item properties change after the
        component is initially rendered.
      </li>
    </ul>
  </>
);
