// @ts-nocheck
import { h } from 'preact';

export const thematicMapAnimationDescription = (
  <>
    <p>
      Applications can utilize the built-in data change animations when updating area or point data. In
      this demo, when the election year changes, the new election year data will cause some areas to
      transition to a different color. Current data change animations support animation of color,
      rotation, and location updates.
    </p>
  </>
);
