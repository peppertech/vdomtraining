// @ts-nocheck
import { h } from 'preact';

export const sunburstDefaultDescription = (
  <>
    <p>A sunburst is an interactive data visualization in which hierarchical data is represented in concentric rings.
    Each ring segment is proportionally sized relative to the other segments at a given level.</p><p>This demo shows the JET Sunburst.</p>
    <ul>
      <li>
        Sunbursts display 2 dimensions of hierarchical data using the size and color of the nodes.
      </li>
      <li>
        Displays the entire hierarchy, so sunburst is preferred over treemap when the user may be
        interested in the higher level information.
      </li>
      <li>To emphasize leaf node data, consider using a treemap instead.</li>
    </ul>
  </>
);
