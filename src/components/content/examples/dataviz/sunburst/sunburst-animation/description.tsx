// @ts-nocheck
import 'preact';

export const sunburstAnimationDescription = (
  <>
    <p>A sunburst is an interactive data visualization in which hierarchical data is represented in concentric rings.
    Each ring segment is proportionally sized relative to the other segments at a given level.</p><p>Click the buttons to animate the sunburst.</p>
  </>
);
