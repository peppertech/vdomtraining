// @ts-nocheck
import { h } from 'preact';

export const sunburstPopupDescription = (
  <>
    <p>A sunburst is an interactive data visualization in which hierarchical data is represented in concentric rings.
    Each ring segment is proportionally sized relative to the other segments at a given level.</p><p>
      This demo shows how to trigger popups when clicking on sunburst nodes using a simple oj-popup.
      Applications however, should only use popups instead of built-in component tooltips if additional
      interaction is needed and not just for textual information. Tooltips should be used for
      non-interactive content.
    </p>
  </>
);
