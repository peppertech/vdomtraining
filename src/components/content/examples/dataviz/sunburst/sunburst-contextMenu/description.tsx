// @ts-nocheck
import { h } from 'preact';

export const sunburstContextMenuDescription = (
  <>
    <p>A sunburst is an interactive data visualization in which hierarchical data is represented in concentric rings.
    Each ring segment is proportionally sized relative to the other segments at a given level.</p><p>
      This demo shows how to trigger context menus on sunburst nodes. Right click on a node and select
      an action. The context menu can also be accessed by keyboard. Press Shift + F10 to open the
      context menu after navigating to a node. Select one of the actions by navigating to it and
      pressing Enter.
    </p>
  </>
);
