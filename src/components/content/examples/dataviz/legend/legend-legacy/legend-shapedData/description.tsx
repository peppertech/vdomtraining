import { h } from 'preact';

export const legendShapedDataDescription = (
  <>
    <p>
      A legend displays an interactive description of symbols, colors, and other
      cues used in graphical information representations.
    </p>
    <p>
      This demo shows how to create a legend with a dataProvider that contains data
      already shaped for the component. Shaped data can be provided for complex legends
      containing multiple sections as well as simple legends that contain only items.
    </p>
  </>
);
