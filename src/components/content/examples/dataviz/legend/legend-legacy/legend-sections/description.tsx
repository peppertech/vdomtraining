import { h } from 'preact';

export const legendSectionsDescription = (
  <>
    <p>
      A legend displays an interactive description of symbols, colors, and other
      cues used in graphical information representations.
    </p>
    <p>
      Legends can display multiple sections of marker-label pairs, and they support
      custom title headers for individual sections.
    </p>
    <p>
      When using a legend to display related information from a separate DVT component,
      consider using attribute group handlers to efficiently set style properties on
      corresponding items in both components.
    </p>
  </>
);
