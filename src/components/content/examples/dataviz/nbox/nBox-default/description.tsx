// @ts-nocheck
import { h } from 'preact';

export const nBoxDefaultDescription = (
  <>
    <p>NBox is an interactive data visualization (typically found in Human Capital Management applications) in which employees are grouped and compared across two dimensions.  Each dimension can be split into multiple ranges.</p><p>
      NBox is useful for measuring data items (e.g. employees) across two dimensions (e.g. potential and
      performance). Each dimension can be split into multiple ranges, whose intersections result in
      distinct cells which data items can be placed into.
    </p>
  </>
);
