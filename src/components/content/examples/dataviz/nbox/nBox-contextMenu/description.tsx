// @ts-nocheck
import { h } from 'preact';

export const nBoxContextMenuDescription = (
  <>
    <p>NBox is an interactive data visualization (typically found in Human Capital Management applications) in which employees are grouped and compared across two dimensions.  Each dimension can be split into multiple ranges.</p><p>
      This demo shows how to trigger context menus on the NBox. Right click on the NBox and select an
      action. The context menu can also be accessed by keyboard. Press Shift + F10 to open the context
      menu after navigating to an item. Select one of the actions by navigating to it and pressing
      Enter.
    </p>
  </>
);
