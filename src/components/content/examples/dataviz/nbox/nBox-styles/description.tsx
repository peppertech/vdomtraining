// @ts-nocheck
import { h } from 'preact';

export const nBoxStylesDescription = (
  <>
    <p>NBox is an interactive data visualization (typically found in Human Capital Management applications) in which employees are grouped and compared across two dimensions.  Each dimension can be split into multiple ranges.</p><p>
      Nodes, cells, labels, and titles can be styled globally via CSS, per component instance using the
      {" "}
      <i><b>styleDefaults</b></i>
      , or if data dependent, individually within the node or cell options. This demo shows how to style
      nodes, cells, labels, and titles at the component level.
    </p>
  </>
);
