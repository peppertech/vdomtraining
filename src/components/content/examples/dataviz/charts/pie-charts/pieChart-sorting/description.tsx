// @ts-nocheck
import { h } from 'preact';

export const pieChartSortingDescription = (
  <>
    <p>A chart displays information graphically, making relationships among the data easier to understand.</p><p>
      Pie chart slices can be sorted based on the values. Small slices can be aggregated into an "Other"
      node using the
      <b><i>other-threshold</i></b>
      attribute, which specifies the smallest slice that will be displayed as a ratio of the total pie
      value.
    </p>
  </>
);
