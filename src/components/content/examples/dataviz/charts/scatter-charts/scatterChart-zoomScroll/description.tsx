// @ts-nocheck
import { h } from 'preact';

export const scatterChartZoomScrollDescription = (
  <>
    <p>A chart displays information graphically, making relationships among the data easier to understand.</p><p>Zoom and scroll the chart with the following gestures:</p>
    <ul>
      <li>Pan scroll the chart by dragging on the plot area.</li>
      <li>
        Use the mouse wheel to zoom in or out. On touch devices, use the two-finger pinch gesture.
      </li>
      <li>
        Click on the zoom button that appears when you hover over the plot area. Drag to draw a
        rectangle and perform a marquee zoom.
      </li>
    </ul>
    <p>
      Use the toggle buttons to enable or disable zooming. Disabling zooming is useful when one wants to
      show a constant viewport size.
    </p>
  </>
);
