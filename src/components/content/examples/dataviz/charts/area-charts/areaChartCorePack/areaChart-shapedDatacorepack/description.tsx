import 'preact';

const descriptionHtmlText = String.raw`<p>An area chart displays information graphically using lines and filled areas, making relationships among the data easier to understand.</p><p>
  This demo shows how to create a JET Area Chart with a dataProvider that contains data that has
  already been shaped for the chart.
</p>`;

export const areaChartShapedDatacorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
