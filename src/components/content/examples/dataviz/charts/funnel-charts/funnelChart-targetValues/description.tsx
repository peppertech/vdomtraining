// @ts-nocheck
import { h } from 'preact';

export const funnelChartTargetValuesDescription = (
  <>
    <p>A chart displays information graphically, making relationships among the data easier to understand.</p><p>This demo shows the JET funnel chart component and the usage of target values.</p>
    <p>
      When target values are set on a data item the
      <b><i>value</i></b>
      attribute defines the filled area within the slice and the
      <i><b>target-value</b></i>
      attribute represents the value of the whole slice.
    </p>
  </>
);
