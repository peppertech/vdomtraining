// @ts-nocheck
import { h } from 'preact';

export const chartPopupDescription = (
  <>
    <p>A chart displays information graphically, making relationships among the data easier to understand.</p><p>
      This demo shows how to trigger popups when clicking chart data items using an oj-popup element.
      Applications however, should only use popups instead of built-in component tooltips if additional
      interaction is needed and not just for textual information. Tooltips should be used for
      non-interactive content.
    </p>
  </>
);
