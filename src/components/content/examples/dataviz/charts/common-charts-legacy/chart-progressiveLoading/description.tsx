// @ts-nocheck
import { h } from 'preact';

export const chartProgressiveLoadingDescription = (
  <>
    <p>A chart displays information graphically, making relationships among the data easier to understand.</p><p>
      This demo shows the progressive loading indicator when the data provider fetch takes longer than a
      certain time. The appearance of the progressive loading indicator varies by chart types,
      coordinate systems, and aspect ratios.
    </p>
  </>
);
