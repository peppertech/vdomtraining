// @ts-nocheck
import { h } from 'preact';

export const chartCustomMarkersDescription = (
  <>
    <p>A chart displays information graphically, making relationships among the data easier to understand.</p><p>
      This demo shows how to create a chart with custom SVG Paths. Custom markers can be specified on
      the chart using SVG path data or images. When SVG path data is used to specify the shapes, the
      chart will style the markers in the same way as its built-in shapes, supporting APIs like color
      and borderColor. Images are most useful for multi-color markers or custom selection effects. For
      more details, see
      <a href={"#"}>this demo</a>
      .
    </p>
  </>
);
