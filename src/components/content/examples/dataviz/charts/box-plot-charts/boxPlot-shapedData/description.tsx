// @ts-nocheck
import { h } from 'preact';

export const boxPlotShapedDataDescription = (
  <>
    <p>
      This demo shows how to create a JET Box Plot with a dataProvider that contains data that has
      already been shaped for the chart. Click the toggle buttons below the chart to change the
      orientation or stack the items.
    </p>
  </>
);
