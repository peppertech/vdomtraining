// @ts-nocheck
import { h } from 'preact';

export const timelineValueFormatsDescription = (
  <>
    <p>A timeline is an interactive data visualization that displays a series of events in chronological order.</p><p>
      This demo shows timeline’s support for formatting of tooltip labels, as well as the ability to
      customize which values are displayed. The default tooltip content differ based on the timeline’s
      item type. Choose among the item type options to see what controls apply to each item type. Toggle
      the checkboxes to hide/show contents in tooltips. Use the input texts to change the name of the
      corresponding tooltip row.
    </p>
  </>
);
