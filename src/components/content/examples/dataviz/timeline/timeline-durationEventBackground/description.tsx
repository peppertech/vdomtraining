// @ts-nocheck
import { h } from 'preact';

export const timelineDurationEventBackgroundDescription = (
  <>
    <p>A timeline is an interactive data visualization that displays a series of events in chronological order.</p><p>
      The background feature allows a user to have a visual for different event types through background
      colors. A stripe is added for event types of
      <b><i>'duration-event'</i></b>
      . To ensure accessibility, it is required for use cases such as this where a color difference is
      used to convey information, to add visible text description in addition to the background color.
    </p>
  </>
);
