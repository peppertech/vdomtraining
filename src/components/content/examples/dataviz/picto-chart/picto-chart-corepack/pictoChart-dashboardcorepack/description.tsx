// @ts-nocheck
import { h } from "preact";

const descriptionHtmlText = String.raw`<p>A picto chart is an interactive data visualization of textual data. PictoChart uses icons to visualize an absolute number, or the relative sizes of the different parts of a population.</p><p>This demo shows how the JET Picto Chart can be used for dashboard purposes.</p>`;

export const pictoChartDashboardcorepackDescription = (
  <div dangerouslySetInnerHTML={{ __html: descriptionHtmlText }} />
);
