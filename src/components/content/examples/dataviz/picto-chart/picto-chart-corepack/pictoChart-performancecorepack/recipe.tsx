// @ts-nocheck
import { h } from "preact";

const recipeHtmlText = String.raw``;

export const pictoChartPerformancecorepackRecipe = (
  <div dangerouslySetInnerHTML={{ __html: recipeHtmlText }} />
);
