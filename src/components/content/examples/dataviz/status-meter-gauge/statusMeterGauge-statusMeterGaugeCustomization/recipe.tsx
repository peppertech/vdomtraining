// @ts-nocheck
import { h } from 'preact';

export const statusMeterGaugeStatusMeterGaugeCustomizationRecipe = (
  <>
    <ol>
      <li>
        Create a status meter gauge by using the oj-status-meter-gauge tag with the
        <i><b>value</b></i>
        attribute defined.
      </li>
      <li>
        Choose the size of the gauge shapes with the
        <i><b>size</b></i>
        attribute. For custom size, set
        <i><b>size</b></i>
        to
        <i>fit</i>
        and set custom size with css class or inline style.
      </li>
      <li>
        Cutomize the indicator and plot area color according to gauge value using the
        <i><b>thresholds</b></i>
        attribute.
      </li>

      <li>
        Cutomize the width of the circular gauge bar using the
        <i><b>inner-radius</b></i>
        attribute.
      </li>
      <li>
        Add reference lines for a particular values in plot area using the
        <i><b>reference-lines</b></i>
        attribute.
      </li>
    </ol>
  </>
);
