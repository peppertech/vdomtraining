import 'preact';

export const colorPaletteDescription = (
  <>
    <p>
      This recipe demonstrates the legacy <code>oj-color-palette</code>{" "}
      example in the recipe layout.
    </p>
    <p>
      The demo follows the cookbook color palette examples and focuses on
      palette rendering, swatch sizing, and selection behavior.
    </p>
  </>
);

export const colorPaletteRecipe = (
  <ol>
    <li>Add an <code>oj-color-palette</code> with a palette data source.</li>
    <li>
      Configure swatch sizing and layout so users can compare palette display
      options.
    </li>
    <li>
      Bind the selected value and surface it in the page to verify interactions.
    </li>
  </ol>
);
