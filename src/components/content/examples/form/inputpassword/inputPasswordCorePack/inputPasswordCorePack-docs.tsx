import { h } from "preact";

export const inputPasswordCorePackDescription = (
  <>
    <p>
      This recipe shows the core pack <code>oj-c-input-password</code> example
      in the standard recipe format.
    </p>
    <p>
      It follows the core pack cookbook examples for states, clear icon support,
      and width configuration.
    </p>
  </>
);

export const inputPasswordCorePackRecipe = (
  <ol>
    <li>Add an <code>oj-c-input-password</code> field.</li>
    <li>
      Configure value, state, and width-related props to match the form layout.
    </li>
    <li>
      Add clear icon or assistance props when you need a richer password entry
      experience.
    </li>
  </ol>
);
