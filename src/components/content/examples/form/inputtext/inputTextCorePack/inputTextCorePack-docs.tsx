import { h } from "preact";

export const inputTextCorePackDescription = (
  <>
    <p>
      This recipe presents the core pack <code>oj-c-input-text</code> example
      in the recipe layout.
    </p>
    <p>
      It follows the core pack cookbook examples for plain text entry, slots,
      prefix or suffix content, raw values, max length, alignment, and virtual
      keyboard behavior.
    </p>
  </>
);

export const inputTextCorePackRecipe = (
  <ol>
    <li>Add an <code>oj-c-input-text</code> field.</li>
    <li>
      Configure value, label, and optional slot or prefix or suffix content.
    </li>
    <li>
      Use states and assistance props to show the main text-entry variations in
      one recipe page.
    </li>
  </ol>
);
