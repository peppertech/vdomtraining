// @ts-nocheck
import { h } from 'preact';

export const helpHintsMessagingValidatorHintsMessagesDescription = (
  <>
    <p>
      The demo shows where the validator hint text and validator error messages are displayed on the
      component.
    </p>

    <p>
      To view the hint set focus on the input. The validator hints are displayed under the field. Only
      one type of hint shows under the field, so if the component has help.instruction, that will take
      precedence over the validator hints.
    </p>

    <p>
      Error messages generated during component validation are shown inline by default. Enter 'a' in any
      field and tab-off.
    </p>
  </>
);
