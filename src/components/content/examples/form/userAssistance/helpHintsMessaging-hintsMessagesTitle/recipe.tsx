// @ts-nocheck
import { h } from 'preact';

export const helpHintsMessagingHintsMessagesTitleRecipe = (
  <>
    <ul>
      <li>
        Set the
        <code className={"prettyprint"}>display-options</code>
        attribute on the component to a JSON object, to control which hint or messaging content do not
        get shown; e.g., to show messages nothing else set -
        <br />
        <code className={"prettyprint"}>
          displayOptions='{'{'}"converterHint": "none", "validatorHint": "none"{'}'}'
        </code>
      </li>
    </ul>
  </>
);
