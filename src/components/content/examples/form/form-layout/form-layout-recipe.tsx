export const formLayoutBasicRecipe = (
  <ol>
    <li>
      Create an <code>oj-form-layout</code> element.
    </li>
    <li>
      Configure layout attributes such as <code>maxColumns</code>,{" "}
      <code>direction</code>, and <code>labelEdge</code> to control the form
      structure.
    </li>
    <li>
      Add form controls with <code>labelHint</code> so labels are generated
      automatically for each label and value pair.
    </li>
    <li>
      Use <code>oj-label-value</code> with <code>colspan</code> when a field or
      block of content should span multiple columns.
    </li>
  </ol>
);
