export const formLayoutBasicDescription = (
  <>
    <p>
      Form Layout arranges groups of label and value pairs in an organized,
      responsive structure.
    </p>
    <p>
      Components with a <code>labelHint</code> can be placed directly inside{" "}
      <code>oj-form-layout</code> and the layout will generate labels
      automatically. Use <code>oj-label-value</code> when content needs custom
      label and value placement or must span multiple columns.
    </p>
  </>
);
