import 'preact';

export const validatorsRequiredValidatorRecipe = (
  <>
    <ul>
      <li>
        Set the{' '}
        <code className={"prettyprint"}>required</code>{' '}
        option on an editable component to use the built-in RequiredValidator.
      </li>
      <li>
        Customize the required message with{' '}
        <code className={"prettyprint"}>required-message-detail</code>{' '}
        when you need application-specific text.
      </li>
    </ul>
  </>
);
