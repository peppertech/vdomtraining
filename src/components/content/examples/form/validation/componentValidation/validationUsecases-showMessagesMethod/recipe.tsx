import { h } from 'preact';

export const validationUsecasesShowMessagesMethodRecipe = (
  <>
    <ul>
      <li>
        Call the component's
        {' '}<code className={"prettyprint"}>showMessages</code>{' '}
        method from your script like this -
        {' '}<code className={"prettyprint"}>document.getElementById('username').showMessages();</code>
      </li>
    </ul>
  </>
);
