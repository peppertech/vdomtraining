import { h } from 'preact';

export const validationUsecasesRefreshMethodRecipe = (
  <>
    <ul>
      <li>
        Call the component's
        {' '}<code className={"prettyprint"}>refresh</code>{' '}
        method from your script like this -
        {' '}<code className={"prettyprint"}>document.getElementById('username').refresh();</code>
      </li>
    </ul>
  </>
);
