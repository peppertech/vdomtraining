import 'preact';

export const validationUsecasesValidateMethodDescription = (
  <>
    <p>
      This demonstrates the
      {' '}<code className={"prettyprint"}>validate</code>{' '}
      method. The validate method returns a Promise. It updates the
      {' '}<code className={"prettyprint"}>valid</code>{' '}
      property and automatically shows any validation error messages.
    </p>
  </>
);
