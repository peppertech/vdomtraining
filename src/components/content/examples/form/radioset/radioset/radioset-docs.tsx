export const radiosetDescription = (
  <>
    <p>
      This recipe wraps the legacy <code>oj-radioset</code> examples in the
      recipe layout.
    </p>
    <p>
      It now points to the grouped legacy radioset examples under the updated
      folder structure.
    </p>
  </>
);

export const radiosetRecipe = (
  <ol>
    <li>Add an <code>oj-radioset</code> with a defined option list.</li>
    <li>Bind the selected value and update it as the user changes selection.</li>
    <li>Add state, help, or validation variations for common usage patterns.</li>
  </ol>
);
