import { h } from "preact";

export const richRadioSetDescription = (
  <>
    <p>
      This recipe presents the core pack <code>oj-c-rich-radioset</code>{" "}
      example in the recipe layout.
    </p>
    <p>
      It follows the cookbook rich radioset examples for basic cards, layout
      options, user assistance, and state variations.
    </p>
  </>
);

export const richRadioSetRecipe = (
  <ol>
    <li>Add an <code>oj-c-rich-radioset</code> with card-style options.</li>
    <li>
      Configure layout and selected value handling so the cards respond cleanly
      to user selection.
    </li>
    <li>
      Add help, messages, or readonly states when the radioset needs richer form
      guidance.
    </li>
  </ol>
);
