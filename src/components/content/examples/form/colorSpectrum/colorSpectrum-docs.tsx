import 'preact';

export const colorSpectrumDescription = (
  <>
    <p>
      This recipe presents the legacy <code>oj-color-spectrum</code> example in
      the recipe layout.
    </p>
    <p>
      It takes cues from the cookbook spectrum and custom validation examples
      to show interactive color selection with validation-ready behavior.
    </p>
  </>
);

export const colorSpectrumRecipe = (
  <ol>
    <li>Add an <code>oj-color-spectrum</code> component.</li>
    <li>
      Bind the selected color so changes can be reflected in companion UI.
    </li>
    <li>
      Add validation or messaging when you need to constrain accepted color
      values.
    </li>
  </ol>
);
