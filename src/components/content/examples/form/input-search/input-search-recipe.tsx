export const inputSearchBasicRecipe = (
  <ol>
    <li>
      Create an <code>oj-input-search</code> element and provide an{" "}
      <code>aria-label</code>.
    </li>
    <li>
      Optionally bind the <code>suggestions</code> property to a data provider
      so the dropdown can show suggested matches.
    </li>
    <li>
      Listen for the <code>ojValueAction</code> event using the{" "}
      <code>onojValueAction</code> attribute to react when the user submits a
      search.
    </li>
  </ol>
);
