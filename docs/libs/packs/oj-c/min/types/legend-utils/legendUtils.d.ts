import type { SizeHandle } from 'oj-c/legend/legend';
export declare const getPreferredSize: (element: HTMLElement & SizeHandle, width: number, height: number) => {
    width: number;
    height: number;
};
